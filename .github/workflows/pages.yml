name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@desktop_v1
      - name: Prepare static site
        run: |
          mkdir -p .pages-build
          rsync -a --copy-links \
            --exclude node_modules \
            --exclude .git \
            --exclude .cursor \
            --exclude .pages-build \
            ./ .pages-build/
          test -f .pages-build/desktop_v1/index.html
          test -f .pages-build/desktop_v1/css/tokens.css
          test -f .pages-build/desktop_v1/index.html
          test -f .pages-build/desktop_v1/css/tokens.css
      - uses: actions/configure-pages@desktop_v1
      - uses: actions/upload-pages-artifact@desktop_v1
        with:
          path: .desktop_v1
      - id: deployment
        uses: actions/deploy-pages@desktop_v1
